#include <stdio.h>
float sumf(float a, float b);
int sumi(int a, int b);

int main(int argc, char *argv[])
{
        sumi(1,2);
        sumf(1.3,2.6);
        return 0;
}
        
float sumf(float a, float b);
{       
        return (float) (a+b);
}       

int sum ( int a, int b)
{
	return (int) (a+b);
}
