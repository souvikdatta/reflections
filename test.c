#include <stdio.h>
float sum(float a, float b);

int main(int argc, char *argv[])
{
        sum(1,2);
        return 0;
}
        
float sum(float a, float b);
{       
        return (float) (a+b);
}       
~ 
